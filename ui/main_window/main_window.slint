import { LSidebar } from "l_sidebar.slint";
export component MainWindow inherits Rectangle {
    height: 790px;
    width: 1600px;
    background: #25272D;
    HorizontalLayout {
        LSidebar { }
    }
}
