import { SessionCollection } from "left_components/session.slint";

// 左侧会话栏
export component LSidebar inherits Rectangle {

    width: 240px;
    background: #2B2E36;
    
    SessionCollection { 
        width: parent.width;
        height: parent.height;
    }
}
