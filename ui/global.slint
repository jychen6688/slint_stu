export global WindowStore {
    callback min();
    callback max();
    callback close();

    // 底部导航栏
    callback sessions();
    callback servers();
}

export global DragWindow {
    callback drag_window(x: length, y: length);
}
